import React from 'react';

const HRScreen = ({ game }) => {
  return (
    <div className="space-y-8">
      <h2 className="text-3xl font-bold text-white">🧑‍💼 Risorse Umane</h2>
      <div className="bg-white bg-opacity-95 backdrop-blur-lg rounded-2xl p-8">
        <h3 className="text-2xl font-semibold mb-6">👥 Il Tuo Team</h3>
        <div className="grid md:grid-cols-2 gap-6">
          <div>
            <h4 className="font-medium text-gray-700 mb-3">Developers ({game?.developers?.length || 0})</h4>
            <div className="space-y-2">
              {game?.developers?.map(dev => (
                <div key={dev.id} className="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                  <span>{dev.name} ({dev.seniority}/5)</span>
                  <span className="text-sm text-gray-600">€{dev.salary?.toLocaleString()}/mese</span>
                </div>
              ))}
            </div>
          </div>
          <div>
            <h4 className="font-medium text-gray-700 mb-3">Commerciali ({game?.salesPeople?.length || 0})</h4>
            <div className="space-y-2">
              {game?.salesPeople?.map(sales => (
                <div key={sales.id} className="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
                  <span>{sales.name} ({sales.experience}/5)</span>
                  <span className="text-sm text-gray-600">€{sales.salary?.toLocaleString()}/mese</span>
                </div>
              ))}
            </div>
          </div>
        </div>
        <div className="mt-6 pt-4 border-t flex justify-between items-center">
          <span className="font-semibold text-lg">Costi Totali Mensili:</span>
          <span className="text-xl font-bold text-red-600">€{game?.monthly_costs?.toLocaleString()}</span>
        </div>
        <div className="mt-8 p-6 bg-blue-50 rounded-xl">
          <p className="text-blue-700">💡 Mercato delle assunzioni in arrivo presto!</p>
        </div>
      </div>
    </div>
  );
};

export default HRScreen;
