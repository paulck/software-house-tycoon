import React from 'react';

const SalesScreen = ({ game }) => {
  return (
    <div className="space-y-8">
      <h2 className="text-3xl font-bold text-white">💼 Sales</h2>
      <div className="bg-white bg-opacity-95 backdrop-blur-lg rounded-2xl p-8">
        <h3 className="text-2xl font-semibold mb-6">🎯 Commerciali ({game?.salesPeople?.length || 0})</h3>
        <div className="space-y-4">
          {game?.salesPeople?.map(sales => (
            <div key={sales.id} className="p-6 border rounded-xl bg-green-50 border-green-200">
              <div className="font-semibold text-lg text-gray-800 mb-2">{sales.name}</div>
              <div className="text-gray-600 mb-3">
                <div>Esperienza: <span className="font-medium">{sales.experience}/5</span></div>
                <div>Stipendio: <span className="font-medium">€{sales.salary?.toLocaleString()}/mese</span></div>
              </div>
              <div className="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
                ✅ Disponibile
              </div>
            </div>
          ))}
        </div>
        <div className="mt-8 p-6 bg-blue-50 rounded-xl">
          <p className="text-blue-700">💡 Funzionalità di generazione progetti in arrivo presto!</p>
        </div>
      </div>
    </div>
  );
};

export default SalesScreen;
